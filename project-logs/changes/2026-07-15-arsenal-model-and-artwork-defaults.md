# 2026-07-15 阿仙奴模型与贴花默认位置

## 目标

替换独立 3D 定制器的球衣展示模型，并修复同区域贴花重叠造成的“点击无反应”错觉。

## 新增

- `public/models/arsenal-jersey.glb`：用户提供的阿仙奴 GLB 2.0 模型。
- 文件大小：13,605,196 bytes；SHA-256：`10EC29505A434AAB65E54E34016F9AC18AB9826D039501CBE919571CBDBC32CB`。
- 布料目标网格选择：优先 `Cloth_mesh` / `fabric` / `body` 名称，排除 `Topstitch_*` 缝线网格。

## 变更

- 展示标题改为 `Arsenal Match Jersey`，内部产品 ID 仍为 `fn8788-jersey`，因此旧设计 JSON 仍可导入。
- Artwork 和文字印花命中逻辑只使用布料网格。
- 新增贴花默认选择未占用的真实网格位置，不再全部压在同一区域中心。

## 风险与回退

新模型约为旧模型的 22 倍，首次加载会更慢。旧 `fn8788-jersey.glb` 未删除；若公开站验收异常，可回退 `showcase` 的后续合并提交，不需要改写历史。

## 验证

- 自动化与构建验证结果在最终提交前更新。
- 公共 URL 视觉验收待用户批准发布后执行。
