# 1.0.0 Shopify 应用首页验收

## 自动验证

- `npm test`：113 个测试文件、1568 项测试通过。
- `npm run build`：通过。
- OAuth 已安装店铺分支：返回 302、设置短期安全会话 Cookie，并保留 Shopify `host` 参数后跳转 `/app`。
- 商家页测试确认版本、订单入口和正式页面文案存在，旧“下一阶段开放”占位文案不存在。

## 真实链路

1. 登录 Shopify Admin 的 `testcsj-secure-bundle` 开发店。
2. 从“应用”菜单打开 `Secure Jersey Configurator Dev`。
3. Shopify 签名入口成功跳转至固定 workers.dev 的 `/app`。
4. 页面显示 `3D 球衣定制管理`、`v1.0.0`、`店铺已连接`、`结账功能 已启用` 和 `生产流程 订单绑定就绪`。
5. 现有商品 `Custom 3D Football Jersey`、S/M/L/XL 映射及附加价映射均正常读取。
6. “查看定制订单”链接保持为 `/admin/`，没有执行配置写入或订单修改。

结论：1.0.0 应用打开页及安全入口验收通过。
